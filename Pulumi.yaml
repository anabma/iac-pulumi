name: client-infrastructure
description: Client Infrastructure with Pulumi on AWS (equivalent of Terraform IBM Cloud setup)
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
